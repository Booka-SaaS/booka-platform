{
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist/booka-frontend/browser",
  "installCommand": "cd frontend && CYPRESS_INSTALL_BINARY=0 npm ci",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
