import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.booka.app',
  appName: 'Booka',
  webDir: 'dist/booka-frontend/browser',
};

export default config;
